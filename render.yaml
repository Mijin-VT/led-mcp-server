services:
  - type: web
    name: led-mcp-server
    runtime: python
    pythonVersion: "3.11"
    buildCommand: "pip install -e ."
    startCommand: "python -m led_mcp_server"
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
